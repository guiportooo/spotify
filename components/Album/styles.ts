import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    width: 150,
    marginHorizontal: 10,
  },
  imageUri: {
    width: '100%',
    height: 150,
  },
  artistsHeadline: {
    color: 'grey',
    marginTop: 10,
  },
})

export default styles
